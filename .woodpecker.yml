when:
  - event: manual

steps:
  - name: build-and-publish
    image: ghcr.io/astral-sh/uv:python3.14-trixie

    environment:
      UV_PUBLISH_TOKEN:
        from_secret: pypi_token

    commands:
      - uv build
      - uv publish
